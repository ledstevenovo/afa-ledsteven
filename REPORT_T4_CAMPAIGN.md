# Tesla T4 训练复盘报告（2026-09-21 → 09-23）

> **一句话结论**：42 个 epoch 的平坦 loss **不是超参问题**，而是"专家组合 + 训练目标"的结构问题。
> 聚合器本身学得又快又对（110 步就饱和），特征级混合的机制也**确实有效**（在部分 prompt 上能同时超过两个单模型），
> 但噪声 MSE 目标把路由指向了"预测噪声最准、美感最差"的 base SD1.5 —— 这一项就吃掉了约 0.26 个美学分点，
> 也解释了为什么 15.5 GPU 小时、42 个 epoch、5 种超参组合之后 loss 与画质都没有改善。

---

## 0. 摘要（TL;DR）

| # | 结论 | 关键数字 |
|---|---|---|
| 1 | 训练 loss 平坦是**结构性的**，与 lr / batch / 精度 / 优化器写法 / 参数化 / 训练时长**全部无关** | 42 epoch：0.157±0.005，趋势 +1.9e-5/epoch；5 种对照实验终点一致 |
| 2 | 聚合器**学得很快**，不是"学不动" | `mean\|attn−0.5\|` 0 → 0.34（250 步）；60k 步只到 0.44 |
| 3 | 原配"第二个专家"是**退化组合**：`sd15_expert_2` 就是 base SD1.5 本身 | UNet 全局 cos-sim **1.00000**，逐张量相对 L2 **0.0000** |
| 4 | 可学习路由空间随**专家数量**单调增长 | 3 专家 +4.63% / 5 专家 +5.75% / 7 专家 **+6.43%**（MSE，跨噪声路由上界） |
| 5 | 但该增益**兑换不到画质**：MSE 最优 ≠ 画质最优 | MSE：SD1.5 0.0572 < RV 0.0589；美学分：SD1.5 **6.008** < RV **6.360** |
| 6 | 机制没问题、**目标函数有问题**：只翻转路由偏好（不重训）就能拿回 ~90% 的画质差距 | 美学分 6.101（训练结果）→ **6.304**（偏好翻成 RV）vs RV 单模型 6.360 |
| 7 | 评测必须换轴：CLIPScore 对这套专家几乎不敏感，**美学分/FID 才能区分** | CLIPScore 差 0.5（噪声内）；美学分差 0.20–0.26（8 个 prompt 中 6 个同向） |
| 8 | 下一台机器（96GB）的**第一优先级是质量对齐的目标函数**，而不是更多专家/更大 batch/更长训练 | 训练目标 110 步饱和，加量不加分 |

---

## 1. 起点：上一轮训练（09-21 → 09-22）

### 1.1 配置

| 项 | 值 |
|---|---|
| 数据 | 1403 张 COCO 照片（`coco_captions_data.json`） |
| 专家 | `Realistic_Vision_V5.1.safetensors` + `sd15_expert_2.safetensors` |
| batch / 分辨率 | 2 / 512（T4 15GB 显存限制：实测占用 11.0GB） |
| 学习率 | epoch 1–27：1e-4 constant；epoch 28–42：5e-5 cosine（warmup 100） |
| 时长 | 22.3 min/epoch（1.91 s/step），42 epoch ≈ **15.5 GPU 小时** |

### 1.2 结果：loss 完全不动

| 区间 | avg_loss |
|---|---|
| epoch 1–10 | 0.15697 ± 0.00623 |
| epoch 11–27 | 0.15786 ± 0.00443 |
| epoch 28–42 | 0.15675 ± 0.00297 |
| **线性趋势** | **+1.85e-5 / epoch**（42 epoch 累计 +0.0008） |

epoch 1 的 0.14634 是全程最低值。**当时被归因为"学习率太高导致 loss 上升"并下调了 lr —— 这个判断从数据上就不成立**（loss 既没上升也没下降）。

---

## 2. 诊断时间线

| 时间 | 动作 | 结论 |
|---|---|---|
| 09-22 傍晚 | 拉出 checkpoint 的历史与权重统计 | 42 epoch 平坦；conv_out 尺度停留在初始化量级附近 |
| 09-22 晚 | 双专家差异测量（CPU） | 两模型功能上确有差异，但方向对不上"谁更好" |
| 09-22 晚 | 固定 batch 的聚合行为对照（trained/uniform/×30/expert0） | 聚合器**有巨大杠杆**，单 batch 内可差 10 倍 → 值得深挖 |
| 09-22 夜 | 四配置判定实验（A 现状 / D 正确 scaler / B 有效 batch 8 / C 聚合器 fp32） | A/D/B 终点**逐位相同**（eval_loss 0.0994x）→ lr/batch/精度/Scaler 全部排除；B 只用 125 步就走完 60k 步的路 |
| 09-22 夜 | 参数位移测量 | 即使零初始化，conv_in/transformer 也在以 ~0.25×lr/步 学习 → "上游冻结"假设**被推翻**（此前是我的监控精度不足导致的误判） |
| 09-22 夜 | 20 组配对的"路由上限"测量 | 该专家对的**上限只有 +0.02%~0.6%** → 训练必然无效 |
| 09-23 凌晨 | 下载论文专家集 + JourneyDB + 多专家框架 | 见 §3.3–3.4 |
| 09-23 02:08–09:29 | **失败的一夜**：158 次崩溃循环，0 个训练步 | 我的梯度检查点 bug（见 §5.1） |
| 09-23 09:42 | 修好后用 3 专家开跑 | 第一次看到 loss 真的下降（60 步 −2.9%） |
| 09-23 11:20 | step-500 checkpoint 生成质量评测 | 聚合器 29.68 vs RV 30.18 vs SD1.5 28.85 |
| 09-23 14:11 | 反事实实验（偏好翻转）+ 美学分评测 | **机制有效、目标有错**（§3.6） |
| 09-23 14:30 | 全量 7 专家扫描 | +6.43%（§3.7） |

---

## 3. 逐项诊断

### 3.1 loss 为什么不动：排除法

| 怀疑点 | 对照实验 | 结果 | 判定 |
|---|---|---|---|
| 学习率 | 5e-5 vs 1e-4 | 终点相同 | ❌ 排除 |
| 有效 batch | 2 vs 8（梯度累积） | 250 步 = 60k 步的终点（eval_loss 0.09942 vs 0.09942）；数据量省 60 倍 | ❌ 排除 |
| 优化器写法 | `optimizer.step()`（GradScaler 未 unscale）vs `scaler.step()+update()` | **逐位相同**（0.09945 vs 0.09944），scale 全程 7e+04 未变 | ❌ 排除（但仍应修） |
| 混合精度 | 聚合器 fp16 前向 vs fp32 前向 | 无差异 | ❌ 排除 |
| 参数化 | `conv_out` 零初始化 vs `out_init_std=0.02` | 150 步终点相同（w0 0.13 vs 0.12） | ❌ 排除 |
| 训练时长 | 250 步 vs 60k 步 | 同一终点 | ❌ 排除 |

**真因**：这对专家在训练目标上的可提升空间只有 ~0.6%，而 epoch 间噪声 sd = 0.005 —— **信号比噪声小一个量级**，loss 曲线永远看不见。

### 3.2 第一个真相：第二个"专家"就是 base SD1.5

```
sd15_expert_2.safetensors vs models/stable-diffusion-v1-5 的 UNet
  → 全局 cos-sim = 1.00000，逐张量相对 L2 = 0.0000（完全一致）
Realistic Vision V5.1 vs base SD1.5 → cos-sim 0.986（就是它的微调）
```

也就是说，原配置是"一个写实微调 + 它自己的父模型"，**没有互补性可言**；AFA 的机制（按 prompt/噪声/位置动态选专家）在这里没有可操作的空间。

### 3.3 怎么量化"有没有可学的东西"：三个指标

| 指标 | 定义 | 性质 |
|---|---|---|
| **整图级 oracle** | 每个样本挑最好的整个专家 | 用了真噪声 → 不可学，仅作参考 |
| **逐元素 oracle** | 每个潜元素挑最接近真噪声的专家 | 用了真噪声 → **不可学**，是绝对上界 |
| **跨噪声路由** | 用同一图像在*其他噪声实现*上的逐元素胜负（多数投票）去路由**留出的**那个噪声 | 只用图像信息 → **可学习上界** |
| 一致性 | 逐元素"谁最优"的身份在不同噪声实现间的一致率，对比独立基线 | 高于基线越多 → 胜负越由图像决定 → 越可学 |

### 3.4 各专家组合的实测结果

| 专家组合 | 整图级 oracle | 逐元素 oracle | **跨噪声路由（可学习）** | 一致性 vs 基线 |
|---|---|---|---|---|
| RV + base SD1.5（2 个） | +0.02% | +10.9% | **≈ 0** | 0.573 vs ~0.51 |
| RV + Counterfeit（二次元） | +0.22% | +13.1% | **≈ 0** | 0.579 vs ~0.51 |
| RV + Ghibli | +0.02% | +10.9% | **≈ 0** | 0.579 vs ~0.51 |
| sd15 + rv + epicrealism（3） | +0.01% | +19.1% | **+4.63%** | 0.487 vs 0.355 |
| 论文 5 专家 | +0.01% | +22.1% | **+5.75%** | 0.387 vs 0.242 |
| 论文 5 专家 + Ghibli + Counterfeit（7） | +0.06% | +25.6% | **+6.43%** | 0.299 vs 0.162 |

> **为什么 2 个专家是零、3 个以上才不是**：两个质量相近的 SD1.5 家族模型之间，"这个位置谁更接近真噪声"接近硬币投掷（一致性仅略高于基线）；一旦候选变多，图像本身对胜负的决定性才浮现出来（一致性显著高于基线）——这正是 AFA 论文所依赖的结构。

### 3.5 生成质量：两条轴

**（a）CLIPScore（文图对齐，CLIP ViT-L/14）**

| 实验 | 变体 | CLIPScore |
|---|---|---|
| 旧 2 专家 checkpoint（12 prompt） | 聚合器 | 30.01 |
| | Realistic Vision 放进 AFA 框架（同 VAE/文本塔） | **31.10** |
| 3 专家 step-500（8 prompt） | 聚合器 | 29.68 |
| | Realistic Vision 单模型 | **30.18** |
| | base SD1.5 单模型 | 28.85 |
| | 聚合器内部硬选 SD1.5 | 28.85 |

**（b）美学分（论文同款 LAION `sac+logos+ava1-l14-linearMSE`）**

| 变体 | 美学分 |
|---|---|
| base SD1.5 单模型 | 6.008 |
| **Realistic Vision 单模型** | **6.360** |
| 训练出的聚合器（MSE 目标 → 偏好 SD1.5） | 6.101 |
| 聚合器内部硬选 SD1.5 | 6.072 |
| **同一权重，仅把输出通道 0/1 互换（偏好翻成 RV）** | **6.304** |

**要点**：CLIPScore 上所有差异都在噪声内（不能区分模型好坏），而美学分**能清晰分层** → 主指标应用美学分（+FID），CLIPScore 只作辅助。

### 3.6 反事实实验：只翻偏好、不重训

把训练好的聚合器的 `conv_out` 输出通道 0 和 1 互换 → "同样的路由策略，但偏好另一个专家"：

| | 训练结果（偏好 SD1.5） | 偏好翻成 RV | RV 单模型 |
|---|---|---|---|
| 美学分 | 6.101 | **6.304** | 6.360 |
| （8 个 prompt 中，翻转后有 6 个变好） | | | |

**结论**：
1. **特征级混合这个机制没问题**——它只是被 MSE 目标带偏了方向；把偏好拨回来就能找回 ~90% 的差距。
2. **混合本身确实能加分**：在 #4/#5 两个 prompt 上，两种聚合器都**同时超过两个单模型**（6.009/6.180 vs 5.831/5.729；5.962/5.993 vs 5.846/5.817）。
3. 但"选哪个模型"几乎没余地：单模型逐 prompt 取最优 = 6.382，仅比"永远用 RV"高 **+0.34%** → **价值在"怎么混合"（逐像素自适应），不在"选谁"**。

> ⚠️ 样本量：8 个 prompt、单 seed；0.20 的均值差统计误差约 0.15 → "方向一致、未达显著"，但 6/8 同向比单看均值更有说服力。

### 3.7 专家数量扫描

| 专家数 | 可学习路由（MSE） | 显存需求（batch 2，无梯度检查点） |
|---|---|---|
| 3 | +4.63% | 13.9 GB ✓ T4 可跑 |
| 5 | +5.75% | >15.4 GB ✗ OOM |
| 7 | **+6.43%** | ~20 GB+ ✗ |

→ 3 专家保住 5 专家 **80%** 的收益、显存只要一半，是 T4 上的最优折中。

---

## 4. 环境与性能实测（T4 15.4GB）

| 项目 | 实测 |
|---|---|
| 显存 | 2 专家 batch 2 = 9.4GB（torch 峰值）；3 专家 = **13.9GB**；5 专家 **OOM**（权重 10.4GB + 搬运峰值） |
| 速度 | 2 专家 batch 2 = **2.03 s/步**；3 专家有效 batch 8 = **9.37 s/步** → 383 步/小时 → 1 epoch（8.4 万图）27.3 h |
| xformers | 装上后**慢 25%**（2.55 vs 2.03 s/步）且不省显存（sm_75 上跑的是次优内核）→ 训练脚本主动 disable |
| cuDNN | **坏的**（系统 cuDNN 9.1 与 torch 自带 9.24 冲突，`CUDNN_STATUS_NOT_INITIALIZED`）；默认 / `LD_LIBRARY_PATH` / `LD_PRELOAD` 三种修法全无效 |
| 梯度检查点 | 3 专家 batch 4 可行（8.3GB）但 **4.83 s/步（2.4× 慢）**；UNet 支持，`Transformer2DModel` **不支持**（直接 raise） |
| 数据 | JourneyDB 经 HF 镜像 `wusize/journeydb`（`repo_type="dataset"`）匿名可得：`captions.zip` 488MB + `images/NNN.tar` 736MB/片（20.9k 张 512×512） |
| 已下载资产 | 5 个论文专家（SD1.5 / Realistic Vision / epiCRealism / majicMIX v6 / AbsoluteReality）+ Ghibli + Counterfeit；JourneyDB **83,757 对**（4 片） |

---

## 5. 踩坑清单（症状 → 原因 → 修复 → 教训）

### 5.1 一夜 158 次崩溃、0 个训练步（最严重）
- **症状**：日志里 158 次 `Traceback`，每次都是加载完模型后立刻退出；wrapper 重试 5 次 → 看门狗每 4 分钟再拉起 → 循环整夜。
- **原因**：我给聚合器的 transformer 也开了梯度检查点，而 diffusers 0.21 的 `Transformer2DModel.enable_gradient_checkpointing()` **直接 raise**：`ValueError: Transformer2DModel does not support gradient checkpointing.`
- **修复**：UNet 仍然开检查点；聚合器部分 try/except 兜住（代码里已固化并注释原因）。
- **教训**：**长跑前必须先验证"能跑起来 + 单步有下降"**。我当时把"加载慢"误当成"还在加载"，直接挂了夜跑——代价是整夜 GPU。
- **次生教训**：日志用 `tee` 时是**块缓冲**，文件会滞后数分钟；判断进程状态要看 `tmux capture-pane`（实时）或直接看进程 CPU/磁盘活动，不要只看日志尾部。

### 5.2 5 专家 CUDA OOM（"卡住"其实是分配器在重试）
- **症状**：进程存活、RSS 13.8GB、GPU 只占 112MB、CPU 14%、几分钟无进展 —— 看起来像"卡死"。
- **原因**：5 个专家权重 10.4GB，加上 fp32→fp16 搬运的临时峰值超过 15.4GB → `torch.OutOfMemoryError`，而缓存的分配器在 OOM 路径上会长时间重试，表现为"假死"。
- **修复**：实测选出**最优 3 专家子集**（+4.63% vs 5 专家 +5.75%，保住 80% 收益）。

### 5.3 停训后被守护进程"复活"，与评测抢显存
- **症状**：为跑评测停掉训练后，几分钟内训练进程又出现，评测与训练同时加载模型。
- **原因**：**三层自动拉起**（wrapper 重试 / 看门狗 / 每小时巡检）中，我只停了前两层的一部分；wrapper 已经 spawn 出的 python 成了孤儿进程（`ppid=1`）继续跑。
- **修复**：按"先停守护（看门狗 + 定时巡检）→ 再杀 wrapper → 最后杀 python 及其 worker"的顺序。
- **教训**：**有自动恢复机制时，"停止"是一个需要精确排序的操作**；停之前先列出所有会拉起它的来源。

### 5.4 cuDNN 不可用 → 白丢 1.5–2× 速度
- **原因**：容器里系统 cuDNN 9.1 与 torch 自带 9.24 冲突，`conv2d` 首次调用即 `CUDNN_STATUS_NOT_INITIALIZED`。
- **验证**：默认、`LD_LIBRARY_PATH` 指向 torch 自带库、`LD_PRELOAD` 全量预载 —— 三种方法都失败；但**关掉 cuDNN 后同一个测试立刻通过**，确认是库冲突而非硬件问题。
- **影响**：训练速度长期打 1.5–2 折；新机器上应默认开启（迁移脚本用 `--cudnn on`）。

### 5.5 xformers 装上反而变慢
- **原因**：T4 是 sm_75，xformers 的 cutlass 内核在这些形状（聚合器 head_dim=16、UNet 40/80/160）上走次优路径；而 PyTorch 2.x 的 SDPA 已经很快。
- **处理**：训练脚本主动 `disable_xformers_memory_efficient_attention()`；README/requirements 注明**不要装**。

### 5.6 我自己的两个错误结论（都被后续测量推翻）
| 错误结论 | 当时的依据 | 为什么错 |
|---|---|---|
| "聚合器停在初始化、混合权重≈0.5" | 用 conv_out 的 std（5e-3）估算 logits 幅度 | 低估了输入激活的量级；实测 `mean\|attn−0.5\|` 早已到 0.44（近乎硬选择） |
| "上游 conv_in/transformer 因零初始化被冻结" | 对比 conv_in 的 RMS 到小数点后 4 位 | **监控分辨率不够**：0.0228 的 RMS 上看不见 2% 的变化；改用相对位移后实测 ~0.25×lr/步 学习正常 |
- **教训**：**诊断指标必须要匹配它要检测的效应量级**；单 batch/单 timestep 的样本也容易得出相反结论（早期"只用 RV 更好"就是这么来的，后来 20 组配对测量显示相反）。

### 5.7 其他
| 症状 | 原因 | 处理 |
|---|---|---|
| `hf_hub_download` 报"仓库不存在/需要认证" | 下载**数据集**时漏了 `repo_type="dataset"` | 加参数即可（不是 gated） |
| 磁盘只剩 4.7GB | 重复模型副本（8.2GB）、旧的 `test_run` 6GB、`modelscope_cache` 6.7GB 残件 | 清理后回到 17GB |
| 云实例把代码/缓存写进系统盘 | `git clone ... afa` 用相对路径 + HF 缓存默认在 `~/.cache` | 资产与 `HF_HOME` 都指到数据盘（`AFA_ASSETS`） |
| 文档时间估计对不上 | `OVERNIGHT.md` 写的 1.4h/epoch 是早期配置 | 实测 22.3 min/epoch；已在迁移文档里更新说明 |
| civitai / github 直连不通 | 网络限制 | civitai 模型从 HF 镜像找对应副本；github 用 SSH（HTTPS 超时） |

---

## 6. 最终结论

### 6.1 关于"为什么训练无效"（已闭环）
1. **不是超参问题**：lr、batch、精度、优化器写法、参数化、训练时长逐一对照排除（§3.1）。
2. **是专家组合问题**：原配的"专家"其实是 base SD1.5 本身，两个质量相近的同族模型之间路由不可学。
3. **是目标函数问题**：噪声 MSE 的最优解 ≠ 画质最优；对这套专家，MSE 偏好 base SD1.5（0.0572 < 0.0589），而美学分偏好 Realistic Vision（6.360 vs 6.008）。MSE 训练出的聚合器因此把 79% 权重压给 SD1.5，画质反而低于最好的单模型。

### 6.2 机制本身是有效的（这是好消息）
- 聚合器 110 步内就学到接近最优的混合策略；
- 只翻转偏好（不重训）就能把美学分从 6.101 拉到 **6.304**（逼近 RV 的 6.360）；
- 在部分 prompt 上，混合输出**同时超过两个单模型**（AFA 论文声称的效应，在这条轴上确实存在）；
- 专家越多，可学习路由空间越大（3 → +4.63%，7 → +6.43%）。

### 6.3 下一步的优先级（已被诊断重排）
| 优先级 | 事项 | 理由 |
|---|---|---|
| **1** | **质量对齐的目标函数**（单步去噪 → VAE 解码 → 美学分/CLIP 反传） | §3.6 已证明它能拿回 ~90% 差距，且混合有"超过单模型"的潜力；15GB 卡装不下，96GB 卡正好解锁 |
| 2 | 主指标换成**美学分（+FID）**，CLIPScore 辅助 | CLIPScore 在这套专家上区分不出成败 |
| 3 | 3 专家即可开跑，5–7 专家留给正式跑 | 80% 收益 / 一半显存 |
| ❌ | ~~更多 epoch / 更大 batch / 更长训练~~ | 训练目标 110 步饱和，加量不加分 |
| ❌ | ~~继续在 SD1.5 家族里换专家~~ | 三次独立筛选都给出一致结论（一致性 ≈ 0.57，可学收益 ≈ 0） |

---

## 7. 复现入口

```bash
# 1) 先筛：这对/这组专家有没有可学的东西（每个专家 ~2 分钟）
python3 probe_multi_expert.py --expert <path> --name <name>
python3 analyze_multi_expert.py            # 判据：跨噪声路由收益 > 2%

# 2) 训练（T4：3 专家 / 有效 batch 8 / 或加 --gradient_checkpointing；96GB：run_pro6000.sh）
python3 train_paper_setting.py --model_files A,B,C \
    --dataset_json_file <journeydb_data.json> \
    --batch_size 2 --grad_accum_steps 4 --resume --model_save_path out

# 3) 评测（**不要**用训练 loss 判断成败）
python3 eval_generation.py --ckpt out --out eval_out --model_files A,B,C \
    --singles "sd15_only=<...>,rv_only=<...>" --swap_experts 0,1   # 反事实对照
python3 score_aesthetics.py eval_out/*/                          # 美学分
```

关键脚本：`train_paper_setting.py`（训练）、`probe_multi_expert.py`+`analyze_multi_expert.py`（筛选）、
`eval_generation.py`（CLIPScore + 反事实）、`score_aesthetics.py`（美学分）、`snapshot_paper.sh`（步数快照）、
`run_paper_setting.sh`/`watchdog_paper.sh`（T4 自动恢复）、`run_pro6000.sh`（96GB 迁移）、`MIGRATION_PRO6000.md`。

---

## 8. 附录：关键原始数据

**8.1 42 epoch 的 avg_loss 抽样**：0.1463, 0.1485, 0.1496, 0.1614, 0.1634, 0.1617, 0.1576, 0.1646, 0.1598, 0.1569 … 0.1581（末）。统计见 §1.2。

**8.2 三配置判定实验（各 250 步，有效 batch 8，固定 eval batch）**

| 配置 | eval_loss 起点 → 终点 | eval\|attn−0.5\| 终点 |
|---|---|---|
| 初始（均匀） | 0.10006 | 0.00 |
| A：batch2 / lr5e-5 / 原优化器写法 | 0.09944 | 0.343 |
| D：A + 正确 `scaler.step()` | 0.09945 | 0.309 |
| B：有效 batch8 / lr1e-4（125 步即达） | 0.09942 | 0.393 |
| 旧配方 60k 步 | 0.09942 | 0.436 |

**8.3 参数位移（20 步，有效 batch 8，lr 1e-4，相对 L2 位移）**

| 参数组 | 零初始化 | `out_init_std=0.02` |
|---|---|---|
| conv_in | 2.2% / 4.2% / 0.36% | 2.5% / 4.6% / 0.33% |
| res_block.conv1 | 3.1% / 2.8% / 5.8% | 3.7% / 3.0% / 3.8% |
| transformer.proj_in | 1.0% / 0.9% / 2.3% | 1.5% / 1.0% / 1.5% |

**8.4 3 专家训练（JourneyDB 83,757 对，512px，有效 batch 8）**

| 步数 | eval_loss | 相对初始化 | w0（expert0 权重，均匀=0.333） |
|---|---|---|---|
| 0 | 0.10225 | — | 0.333 |
| 10 | 0.10175 | −0.49% | 0.352 |
| 110 | 0.09913 | −3.05% | 0.793 |
| 810 | 0.09895 | −3.23% | 0.791 |
| 1190 | 0.09904 | −3.14% | 0.770 |

→ **110 步即饱和**；速度 9.37 s/步（383 步/小时）；1 epoch = 10,469 步 = 27.3 小时。

**8.5 美学分逐 prompt（8 个 prompt，单 seed）**

| prompt | SD1.5 | RV | 聚合器（偏好 SD1.5） | 聚合器（偏好翻成 RV） |
|---|---|---|---|---|
| 0 | 6.078 | 6.355 | 6.217 | **6.363** |
| 1 | 5.874 | 5.939 | 5.907 | 5.827 |
| 2 | 5.495 | **6.930** | 5.533 | 6.216 |
| 3 | 6.599 | 6.558 | 6.543 | **6.754** |
| 4 | 5.831 | 5.729 | 6.009 | **6.180** |
| 5 | 5.846 | 5.817 | 5.962 | **5.993** |
| 6 | 6.276 | 6.682 | 6.564 | **6.743** |
| 7 | 6.069 | 6.871 | 6.075 | 6.355 |
| **均值** | 6.008 | **6.360** | 6.101 | **6.304** |

（#4/#5 两行：两种聚合器都超过两个单模型 → 混合机制本身有增益）
